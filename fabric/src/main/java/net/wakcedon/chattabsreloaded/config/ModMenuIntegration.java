package net.wakcedon.chattabsreloaded.config;

public final class ModMenuIntegration {

    private ModMenuIntegration() {}

    public static void register() {
        // Intentionally left blank; modmenu is an optional dev-time dependency for fabric.
        // When present, fabric build can register the config screen via reflection.
    }
}
